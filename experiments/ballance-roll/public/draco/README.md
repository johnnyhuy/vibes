# Draco decoder

Copied from `three/examples/jsm/libs/draco/gltf` so `useGLTF` can decode the Haze Walk props without fetching gstatic. Decoder + wasm wrapper only — I did not vendor the encoder. Google Draco, Apache-2.0.
